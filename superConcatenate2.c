#include <stdio.h>
#include <stdlib.h> // Para el malloc

char* superConcatenate(char* arreglo[], int cantidad) {
    for(int i = 0; i< cantidad; i++){
        while (arreglo[i][j] != '\0') {

        }
    }

    // Reservar memoria para el resultado
    char* resultado = (char*) malloc((longitudTotal))
}

int main() {

}