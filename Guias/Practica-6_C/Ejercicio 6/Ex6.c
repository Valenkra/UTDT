#include <stdlib.h>
#include <stdio.h>

struct node {
    int data;
    struct node *next;
};

/*
    Implementar la funcion struct node* addLast(struct node* n, int data), que toma un
    puntero al primer nodo de la lista y agrega en el ultimo lugar de la lista un nuevo nodo que
    contiene el dato pasado por parametro. La funcion debe retornar el puntero al primer nodo de
    la lista.

*/

struct node* addLast(struct node* n, int data) {
    struct node* current = node->

    return 0;
}

int main() {

    return 0;
}